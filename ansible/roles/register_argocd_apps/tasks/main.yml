---
# register_argocd_apps role – OCI レジストリ secret と app-of-apps Application を ArgoCD に登録する
# kubernetes.core モジュールで localhost から Kubernetes API に接続する
# (kubeconfig は呼び出し元の module_defaults で指定される)
# push_infra_apps role の後に実行すること

- name: Apply OCI registry secret (argocd)
  kubernetes.core.k8s:
    state: present
    definition:
      apiVersion: v1
      kind: Secret
      metadata:
        name: sakura-container-registry
        namespace: argocd
        labels:
          argocd.argoproj.io/secret-type: repository
      type: Opaque
      stringData:
        name: sakura-container-registry
        type: helm
        url: "{{ container_registry_fqdn }}"
        username: "{{ container_registry_user }}"
        password: "{{ container_registry_password }}"
        enableOCI: "true"
  no_log: true

- name: Apply app-of-apps Application
  kubernetes.core.k8s:
    state: present
    definition:
      apiVersion: argoproj.io/v1alpha1
      kind: Application
      metadata:
        name: app-of-apps
        namespace: argocd
        finalizers:
          - resources-finalizer.argocd.io
      spec:
        project: infra
        source:
          repoURL: "{{ container_registry_fqdn }}"
          chart: "{{ infra_apps_chart_name }}"
          targetRevision: "{{ infra_apps_chart_version }}"
        destination:
          server: https://kubernetes.default.svc
          namespace: argocd
        syncPolicy:
          automated:
            prune: true
            selfHeal: true

- name: Show registration completion
  ansible.builtin.debug:
    msg: >-
      app-of-apps 登録完了:
      oci://{{ container_registry_fqdn }}/{{ infra_apps_chart_name }}:{{ infra_apps_chart_version }}
